import { styled } from "@/stitches.config";

export const Card = styled("div", {
  // background: "black",
  //todo changed color to theme background
  borderRadius: "24px",
  padding: "2px",
  boxShadow: "0 10px 40px rgba(0, 0, 0, 0.1)",
  // todo added quick theme  shadow
});
