import { styled } from "@/stitches.config";
export const Label = styled("label", {
  display: "block",
  fontSize: "0.875rem",
  fontWeight: "600",
  marginBottom: "0.5rem",
  color: "var(--foreground)",
});
