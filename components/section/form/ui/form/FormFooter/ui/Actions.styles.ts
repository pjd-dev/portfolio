"use client";

import { styled } from "@/stitches.config";

export const Actions = styled("div", {
  display: "flex",
  alignItems: "center",
  gap: "0.6rem",
  flexWrap: "wrap",
  justifyContent: "flex-end",
});
