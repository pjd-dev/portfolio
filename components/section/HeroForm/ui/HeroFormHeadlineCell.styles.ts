import { styled } from "@/stitches.config";

export const HeroFormHeadlineCell = styled("div", {
  gridArea: "headline",
  minWidth: 0,
  minHeight: 0,
  display: "flex",
  flexDirection: "column",
  alignItems: "flex-end",
  textAlign: "right",
  gap: "1rem",
  paddingTop: "0.5rem",
});
